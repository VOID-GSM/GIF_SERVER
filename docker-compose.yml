services:
  gif-backend:
    build: .
    container_name: gif-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    extra_hosts:
      - "oauth.authorization.datagsm.kr:3.35.143.88"
